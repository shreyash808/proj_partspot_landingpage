import 'dart:io';

import 'package:get/get.dart';

class AppRoutes {
  static List<GetPage> getRoutes() {
    return [
      // GetPage(name: '/', transition: Transition.circularReveal, page: () => SplashScreen(), binding: SplashBinding()),
    ];
  }
}
